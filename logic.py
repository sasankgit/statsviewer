def calculate_scores(stats: dict) -> dict:
    """
    Takes raw stats (dict of categories: value),
    returns processed averages.
    """
    # Here you can expand with more complex logic later
    return {key: value for key, value in stats.items()}
